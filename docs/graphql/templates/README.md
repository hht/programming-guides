# templates

> 仅允许：SDL stub、env 例、本 README。  
> **禁止**可运行业务实现（无 `.ts` / `.go` / `.py` resolver 模块）。

| 文件 | 用途 |
|------|------|
| [schema.stub.graphql](./schema.stub.graphql) | Schema-first SDL 根形状例（实现仓扩展，勿当业务域终态） |
| [env.example](./env.example) | 环境变量名例；staging/prod 成对、值不入库 |
| [README.md](./README.md) | 本说明 |
