# templates

> 仅允许：JSON Schema、env 例、本 README。  
> **禁止**可运行业务实现（无 `.ts` / `.go` / `.py` 登录模块）。

| 文件 | 用途 |
|------|------|
| [session.schema.json](./session.schema.json) | 会话记录字段例（实现仓迁移对齐） |
| [env.example](./env.example) | 环境变量名例；staging/prod 成对、值不入库 |
| [README.md](./README.md) | 本说明 |
