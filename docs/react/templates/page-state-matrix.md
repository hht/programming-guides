# 页面 × 状态矩阵（INPUTS 填空）

**列说明：**  
- 列表路由：填 `pending|empty|error|success`；`not-found` 列填 N/A  
- 详情路由：填 `pending|error|success|not-found`；`empty` 列填 N/A（404≠empty）  
- `未登录|session unknown` = 路由/会话门闸（`03`/`07`）

| 路由 | pending | empty | error | success | not-found | 未登录 | session unknown |
|------|---------|-------|-------|---------|-----------|--------|-----------------|
| / | | | | | | | |
| | | | | | | | |
