# 目的地 × 状态矩阵（INPUTS 填空）

**列说明：**  
- 列表目的地：填 `loading|empty|error|success`；`not-found` 列填 N/A  
- 详情目的地：填 `loading|error|success|not-found`；`empty` 列填 N/A（404≠empty）  
- 每格写设计 frame 名，或 `同 success + overlay`；必做格禁止空白  

| 目的地 / route | loading | empty | error | success | not-found | 备注（SavedState 键等） |
|----------------|---------|-------|-------|---------|-----------|-------------------------|
| | | | | | | |
| | | | | | | |
| | | | | | | |
