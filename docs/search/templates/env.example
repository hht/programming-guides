# Copy into app env; values never committed.
DATABASE_URL=postgres://app:app@localhost:5432/app
# Optional — only when INPUTS §11 scale trigger selects a dedicated engine:
# SEARCH_ENGINE_URL=
# SEARCH_ENGINE_API_KEY=
# Optional — only when INPUTS §10 enables vectors:
# EMBEDDING_API_URL=
# EMBEDDING_MODEL=
