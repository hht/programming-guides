# Copy to implementation repo; values never committed.
# staging / prod pairs required (INPUTS §9).

APP_ENV=development

# Optional API (if desktop talks to backend)
# VITE_API_BASE_URL=

# Updater (only if INPUTS §15)
# TAURI_UPDATER_ENDPOINT=
# TAURI_UPDATER_PUBKEY=

# Secrets: use OS keychain / CI secrets — do not put real values here
