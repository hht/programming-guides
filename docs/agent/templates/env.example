# Copy to .env — never commit secrets
# Pairing: use same names in DEV/PROD hosts; values differ per env.
AGENT_MODEL=
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
AGENT_EVAL=0
# AGENT_EVAL=1 enables mock provider in tests
AGENT_MAX_TURNS=8
AGENT_MAX_TOOL_CALLS=16
# Optional explicit pair labels (if your host uses prefixes):
# DEV_OPENAI_API_KEY=  PROD_OPENAI_API_KEY=
