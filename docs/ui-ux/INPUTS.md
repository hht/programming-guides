# INPUTS — 缺则停

任一项缺失或应填 N/A 未填 → **停止画屏**。

## 必填

| # | 项 | 验收（合格谓词） |
|---|-----|------------------|
| 1 | **用户任务** | 含：角色 + 动作 + 可观察成功标准；非空 |
| 2 | **平台** | 主平台四选一（字面量固定）：`web` / `ios` / `android` / `macos`（小写；与 Figma `<platform>` 一致） |
| 2b | **导航壳**（按平台必选一） | `web`：□顶栏 □侧栏；`android`：□navigation-bar □navigation-rail □navigation-drawer；`ios`：□tab+stack（默认勾此项，改须写理由）；`macos`：□sidebar+toolbar（默认）；互斥单选，未选=BLOCKED |
| 3 | **关键流程** | ≥1 主路径步骤列表；含成功/失败出口 |
| 3b | **屏幕/Frame 清单** | 表：`screen-id` \| 目的 \| 主 CTA \| 必交付状态（至少 default+loading+error 或注明 N/A） |
| 4 | **内容清单** | 每 `screen-id`：**字段/槽位键名表**（`slot` \| 类型/用途 \| 默认文案或 `dynamic`）。禁止只写笼统「动态」而无槽位键。若 §3b 该屏含 `error`：须另附 **错误文案表**（`code或场景` \| 用户可见文案 \| 下一步）或显式 `N/A`+理由 |
| 5 | **品牌** | 主色：hex 或「系统」；若「系统」且无 DS → 默认主色 `#111111`、字阶正文 16/行高 24（Web）或平台系统正文字号；字体；密度 □紧□标准□松 |
| 6 | **Figma** | □ 新建 □ 已有 URL；命名前缀；□ 有 DS 库 URL / □ 无 DS |
| 7 | **a11y** | 默认 WCAG 2.2 AA；裁剪须写理由 |
| 8 | **保真** | 默认中保真；□线框 □中保真 □高保真（插画非必做） |
| 8b | **Token 落点** | 默认 `design/tokens.md`（须建）；若仅用 Figma Variables：须勾选并保证与 `tokens.md` **同名**，冲突以 `tokens.md` 为准（见 `00`） |

## 门闸等价（设计域）

| 元指南项 | 本域填法 |
|----------|----------|
| API/错误码 | 错误文案表挂 §4（当有 error 态）；纯无错误屏可 `N/A`+理由 |
| 环境成对 | Figma 权限：实现方账号可读；深色若要则 light/dark 成对 |
| 授权策略 | `N/A` 或标注登录墙屏 |

## 若适用

| # | 项 |
|---|-----|
| 9 | 深色模式 □要□不要（不要=N/A） |
| 10 | 多语言（键名进 §4 槽位或另表） |
| 11 | 对接实现指南名（react/next/ios…） |

## 门闸

```text
INPUTS OK
```
