# 10 — 清单

- [ ] `INPUTS OK`；平台互斥已约定（默认 Workers 或写明备选/条件） 
- [ ] `01` 栈：Cloudflare Workers + TS（默认）；未双默认 Vercel/Lambda 
- [ ] `02` Pass1 词表（invoke / response / timeout / retry / 能力路径） 
- [ ] `03` 唯一入口 + compatibility_date 或等价 
- [ ] `04` 触发表 ≥1 HTTP 路径 
- [ ] `05` **Invocation Lifecycle** 编号步骤 
- [ ] `06` 墙钟/CPU 预算数字 
- [ ] `07` 写路径幂等 + 超时/重试策略 
- [ ] `08` 错误映射 + secrets 不入库 
- [ ] 无常驻进程冒充边缘验收 
- [ ] `commands` `check` 绿 
- [ ] `11` **A+B+D** 
