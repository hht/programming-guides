# route × 状态矩阵（INPUTS 填空）

**列说明：** 
- 列表 route：填 `loading|empty|error|success`；`not-found` 列填 N/A 
- 详情 route：填 `loading|error|success|not-found`；`empty` 列填 N/A（404≠empty） 
- 每格写设计 frame 名，或 `同 success + overlay`；必做格禁止空白 
- **失焦取消**：备注列勾选「须 Abort/代次」或 N/A（对齐 INPUTS §9）

| route（app/ 路径） | loading | empty | error | success | not-found | 失焦取消 | 备注（原生模块等） |
|--------------------|---------|-------|-------|---------|-----------|----------|-------------------|
| | | | | | | | |
| | | | | | | | |
| | | | | | | | |
