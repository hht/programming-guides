# App role (no BYPASSRLS). Staging/prod pairs — values not committed.
DATABASE_URL=
DATABASE_URL_STAGING=
DATABASE_URL_PROD=

# Migration owner URL (required when RLS enabled; else may equal DATABASE_URL)
DATABASE_URL_MIGRATE=
DATABASE_URL_MIGRATE_STAGING=
DATABASE_URL_MIGRATE_PROD=
